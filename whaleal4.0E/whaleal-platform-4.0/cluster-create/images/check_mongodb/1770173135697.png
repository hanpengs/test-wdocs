# WAP 在 AWS 上的 MongoDB 备份与恢复流程

本文档描述 Whaleal Platform（WAP）在 AWS 环境中，对自动化部署的 MongoDB 集群所提供的 **备份与恢复整体架构、执行流程及资源规划策略**。

---

## 一、整体架构说明

### 1. 架构组件

WAP 的 MongoDB 备份恢复体系主要由以下组件构成：

- **MongoDB 集群**
  - 复制集或分片集群
  - 提供 oplog 与全量数据源
- **WAP Server**
  - 负责备份策略下发、任务调度、状态管理
- **DDT Server（备份恢复执行节点）**
  - 独立 EC2 实例
  - 实际执行 MongoDB 备份与恢复任务
- **备份存储**
  - 本地磁盘 / 对象存储（按部署策略）

---

### 2. 网络与数据流向

- MongoDB 节点通过 **内网** 与 DDT Server 通信
- 所有备份与恢复数据流 **不经过 WAP Server**
- WAP Server 仅承担：
  - 控制面（Control Plane）
  - 调度、审计、可视化

---

## 二、备份流程（Backup）

### 1. 备份策略配置

点击BackUp下的BackUp，点击Create Repository创建备份策略

![1770085018862](images/Backup_Restore/1770085018862.png)

![1770085091168](images/Backup_Restore/1770085091168.png)

用户在 WAP 平台创建备份仓库（Repository）时，可配置以下参数：

* **Repository Name**

  * 备份仓库名称
* **MongoDB Cluster**

  * 选择需要备份的 MongoDB 集群
* **Take Snapshots Every**

  * 备份周期（按天）
* **Take Snapshots Every**

  * 定时执行时间点
* Init Time

  * 快照备份开始时间使用UTC时间
* **Namespace Whitelist**

  - 支持指定数据库 / 集合
  - 支持白名单方式控制备份范围
* **Recover Data From Days**

  - 保留天数
  - 超期备份自动清理
* Send Message

  * 默认强制发送备份信息
* advanced

  * 备份服务进程内存限时，默认

---

### 2. 备份类型

WAP 采用 **全量 + 增量（oplog）** 组合策略：

#### 2.1 全量备份（Full Backup）

- 周期性执行
- 备份 MongoDB 全量数据
- 用于构建恢复基线

#### 2.2 增量备份（Oplog Backup）

- 基于 MongoDB `oplog.rs`
- 持续或定时抓取
- 用于支持 **时间点恢复（PITR）**

---

### 3. 备份执行流程

1. WAP Server 根据策略触发备份任务
2. 任务下发至对应的 DDT Server
3. DDT Server：
   - 从 MongoDB Secondary 拉取数据
   - 执行全量或 oplog 备份
4. 备份数据写入目标存储
5. 任务执行状态与结果回传至 WAP Server
6. 平台更新备份状态并生成审计记录

---

备份图例

![1770088303319](images/Backup_Restore/1770088303319.png)

## 三、恢复流程（Restore）

点击BackUp下的Restore，点击Restore创建恢复流程

![1770088336100](images/Backup_Restore/1770088336100.png)

### 1. 恢复方式

WAP 支持以下恢复方式：

- **全量恢复**
- **时间点恢复（PITR）**
- **指定库 / 集合恢复**
- **新集群恢复（克隆集群）**

---

### 2. 恢复执行流程

1. 用户在 WAP 平台发起恢复任务
2. 选择：
   - 目标备份集
   - 恢复时间点
   - 目标 MongoDB 集群（原集群 / 新集群）
3. WAP Server 下发恢复任务至 DDT Server
4. DDT Server 执行：
   1. 全量数据恢复
   2. 按时间顺序回放 oplog
5. 恢复完成后执行一致性校验
6. 恢复状态回传至 WAP 平台

![1770088866005](images/Backup_Restore/1770088866005.png)‘

参数说明：

* Backup Strategy
  * 选择备份任务的名称
  * Restore To

    * 要恢复到的集群，可以是自身集群也可以是新集群
  * Restore Type

    * point in time：任意时间点恢复（可以恢复到指定的任意时间点）
    * snapshot：快照点恢复（选择自定义的快照文件恢复）

---

## 四、DDT Server 资源规划与并发模型

### 1. DDT Server 职责

- 执行 MongoDB 全量备份
- 执行 oplog 采集与回放
- 执行恢复任务
- 支持多集群并发任务

---

### 2. 资源分布模型

#### EC2-1（常驻备份执行节点）

- **规格**：8C / 16GB
- **职责**：
  - MongoDB oplog 备份
  - MongoDB 全量备份
  - Java oplog 处理
- **资源占用模型**：
  - Oplog 备份：≈ 2GB
  - 全量备份：≈ 2GB
  - Java 处理：≈ 2GB
- **合计**：≈ 14GB

---

#### EC2-2（恢复专用节点）

- **规格**：4C / 10.5GB（或更高）
- **职责**：
  - 全量恢复
  - oplog 回放
- **单任务内存占用**：
  - ≈ 8GB

---

### 3. 并发与限流策略

- 同一时间：
  - 一个 DDT Server **仅执行一个恢复任务**
  - 避免 IO 与 CPU 抢占
- 当 CPU 使用率 > 80%：
  - 自动暂停新任务调度
- 确保：
  - 单次恢复任务在可控时间内完成
  - 不影响其他备份任务稳定性

---

## 五、分片集群备份说明

- **一个分片集群**：
  - 至少分配 **一个独立 DDT Server**
- 不同 shard 数量对应不同 EC2 规格建议：


| Shard 数量 | DDT Server EC2 规格      |
| ---------- | ------------------------ |
| 2 – 3     | 8C / 16GB（c5.2xlarge）  |
| 4 – 5     | 8C / 21GB（c5n.2xlarge） |
| 6 – 10    | 8C / 30GB（m3.2xlarge）  |

> shard 数量越多，全量备份与恢复时的并发流量与内存需求越高。

---

## 六、设计原则总结

- **控制面与数据面分离**
- **备份不影响主业务**
- **恢复任务严格限流**
- **分片集群独立资源池**
- **支持时间点精确恢复（PITR）**

> WAP 通过标准化的 DDT 执行节点与精细化调度策略，在 AWS 环境中实现 MongoDB 备份与恢复的高可靠、可扩展与可审计交付。
